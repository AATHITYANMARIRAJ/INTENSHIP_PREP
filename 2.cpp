#include <bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cout<<"enter the string\n";
    getline(cin,s);
    stringstream ss(s);
    int count=0;
    int line=0;
    string word;
    while(ss >> word){
        if(word[i])
    }
    cout<<"the no of lines are"<<line<<endl;
    return 0;
}